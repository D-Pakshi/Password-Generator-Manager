# Password Generator

This Python script generates strong passwords using a mix of uppercase, lowercase, numbers, and special characters. It asks for the website name, link, and required password length, then saves the password to a `password.txt` file.

## Features

- Generates a strong, random password.
- Saves the password along with the website details in `password.txt`.

## Requirements

- Python 3.x